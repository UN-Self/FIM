{
  "name": "@fim/protocol",
  "version": "0.1.0",
  "private": true,
  "description": "FIM protocol types — JSON-serializable, zero runtime dependencies",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^4.7.4"
  }
}
